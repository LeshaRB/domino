Project protalForntEnd = project(":portal-frontend")

ext {
  // Spring build dir
  bootJarDir = "BOOT-INF/classes/static"

  // GWT build dir
  buildDirGwt = "${protalForntEnd.buildDir}/gwt"
  extraDirGwt = "${protalForntEnd.buildDir}/extra"
}